---
const projectList = [
  {
    title: "Text Summarizer",
    desc: "A hybrid system for both abstractive and extractive document summarization using NLP techniques.",
    tags: ["NLP", "Python"]
  },
  {
    title: "Book Rec-System",
    desc: "A hybrid book recommendation engine built using KNN and collaborative filtering algorithms.",
    tags: ["ML", "Data Science"]
  }
];
---
<section id="projects" class="py-32 border-t border-white/5">
    <div class="text-blue-500 text-[10px] mb-16 font-bold tracking-[0.4em] uppercase">Projects</div>
    <div class="grid grid-cols-1 gap-12">
        {projectList.map((p) => (
            <div class="group relative">
                <h3 class="text-4xl font-bold text-white mb-4 group-hover:text-blue-500 transition-colors italic">{p.title}</h3>
                <p class="text-zinc-400 text-lg leading-relaxed max-w-2xl mb-6">{p.desc}</p>
                <div class="flex gap-4">
                    {p.tags.map(tag => (
                        <span class="text-[10px] text-zinc-500 font-bold uppercase tracking-[0.2em]">{tag}</span>
                    ))}
                </div>
            </div>
        ))}
    </div>
</section>